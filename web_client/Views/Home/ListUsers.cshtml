
@model List<backend2.User>
@{
    ViewBag.Title = "ListUsers";
}

<h2>ListUsers</h2>
@foreach (var element in Model)
{
    <p>@Html.DisplayFor(m => element)</p>
}
<table>
    <thead>
        <tr><td></td></tr>
    </thead>
    <tbody></tbody>
</table>